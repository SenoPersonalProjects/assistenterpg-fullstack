import { RouteLoadingSkeleton } from '@/components/ui/RouteLoadingSkeleton';

export default function NovoNpcLoading() {
  return <RouteLoadingSkeleton message="Preparando ficha de NPC..." variant="form" />;
}
