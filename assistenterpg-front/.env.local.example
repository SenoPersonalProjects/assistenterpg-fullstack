# Copie este arquivo para .env.local antes de rodar o frontend localmente.
# .env.local sobrepoe .env no Next.js, evitando chamadas acidentais para producao.

NEXT_PUBLIC_API_URL=http://localhost:3000
